'use client'
/**
 * S00_Loading — Scene shell (Phase 1)
 * Full implementation built in Phase 5+
 */
import type { SceneProps } from '@/types/scene.types'

export default function S00_Loading(_props: SceneProps) {
  return null
}
